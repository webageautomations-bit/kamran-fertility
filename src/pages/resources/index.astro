---
import Layout from "../../layouts/Layout.astro";
import PageHero from "../../components/PageHero.astro";
import CloseContact from "../../components/CloseContact.astro";
import { getCollection } from "astro:content";

const posts = (await getCollection("resources")).sort(
  (a, b) => b.data.publishDate.getTime() - a.data.publishDate.getTime(),
);

const fmtDate = (d: Date) =>
  d.toLocaleDateString("en-GB", { day: "numeric", month: "short", year: "numeric" });

const breadcrumbs = [
  { label: "Home", href: "/" },
  { label: "Resources" },
];
---

<Layout
  title="Resources — Dr. Kamran Saleem, Lahore"
  description="Clear, honest fertility education from Dr. Kamran Saleem's clinic in Lahore — plain-language answers on assessment, treatment options, and what to expect."
>
  <PageHero
    eyebrow="Resources"
    title="Clear answers, honestly explained"
    sub="Plain-language fertility education — no jargon, no hard sell. Just the things patients most often want to understand, explained calmly."
    breadcrumbs={breadcrumbs}
    cta="Ask a question on WhatsApp"
  />

  <section class="bg-white py-16 md:py-24">
    <div class="mx-auto max-w-6xl px-5 sm:px-8">
      <div class="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
        {
          posts.map((post, i) => (
            <a
              href={`/resources/${post.id}`}
              class="hover-lift group flex flex-col rounded-[var(--r-md)] border border-line bg-white p-6 shadow-[var(--shadow-soft)]"
              data-reveal
              data-reveal-delay={Math.min(i + 1, 8)}
            >
              <span class="inline-flex w-fit items-center rounded-full bg-azure/10 px-3 py-1 text-xs font-semibold text-azure">
                {post.data.category}
              </span>
              <h2 class="mt-4 font-heading text-lg font-bold text-ink transition-colors group-hover:text-azure">
                {post.data.title}
              </h2>
              <p class="mt-2 flex-1 text-sm leading-relaxed text-body">
                {post.data.description}
              </p>
              <div class="mt-5 flex items-center gap-2 text-xs text-body/70">
                {post.data.readingTime && <span>{post.data.readingTime}</span>}
                {post.data.readingTime && <span aria-hidden="true">·</span>}
                <time datetime={post.data.publishDate.toISOString()}>
                  {fmtDate(post.data.publishDate)}
                </time>
              </div>
            </a>
          ))
        }
      </div>
    </div>
  </section>

  <CloseContact />
</Layout>
